<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IT Academy</title>
    <link rel="stylesheet" href="/css/style.css"/>
</head>
<body>
    <h1>IT Academy</h1>
    <h2>Formations disponibles</h2>
    <div>
        <a href="/panier"><strong>Panier</strong></a>    <a href="/connect"><strong>Se connecter</strong></a>
    </div>
    <div>
        <table>
            <tr>
                <th>id</th><th class="namecss">Nom</th><th>Prix</th><th class="namecss">Début</th><th class="namecss">Fin</th><th></th>
            </tr>

            <% if(tNames.length>0){ %>
                <% for(let i=0; i<tNames.length ; i++){ %> 
                    <tr id=ListeForm<%=i %> class="row" >
                        <td><%= i+1 %></td>
                        <td><%= tNames[i].name %></td>
                        <td><%= tNames[i].prix %></td>
                        <td><%= tNames[i].debut %></td>
                        <td><%= tNames[i].end %></td>
                        <td><a href="/<%=i %> ">S'inscrire</a></td>
                    </tr>
                <% } %>
            <%}else{ %> 
                <p>No Data available</p>
            <% } %>
        </table>
    </div>
</body>
</html>